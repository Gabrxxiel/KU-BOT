{
  "name": "ku-support-bot",
  "version": "1.0.0",
  "description": "Key User Support Bot — Salesforce SFA / ADMOSA Guatemala",
  "main": "api/chat.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
